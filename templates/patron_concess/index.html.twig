{% extends 'base.html.twig' %}

{% block title %}Hello PatronConcessController!{% endblock %}

{% block body %}
<div>
    <h1>Formulaire Véhicule</h1>
    {{ form_start(voiture) }}
        {{ form_widget(voiture) }}
            <button type="submit" class="btn btn-primary text-center">Ajouter un véhicule</button>
    {{ form_end(voiture) }}
</div>
{% endblock %}
